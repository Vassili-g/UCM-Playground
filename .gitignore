node_modules/
dist/

# Variables CSS générées par Style Dictionary depuis tokens.json, à chaque
# `npm run dev` et `npm run build`. On ne versionne pas une sortie dérivée.
src/generated/

# Divers
.DS_Store
*.local
.vite/
