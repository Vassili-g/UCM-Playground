node_modules/
dist/

# La feuille des tokens, écrite par `ucm tokens css` depuis tokens.json à chaque
# `npm run dev` et `npm run build`. On ne versionne pas une sortie dérivée.
src/generated/

# Divers
.DS_Store
*.local
.vite/
# Le rapport de `ucm check --report`, réécrit à chaque exécution.
ci-report.md
