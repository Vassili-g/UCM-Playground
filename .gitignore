node_modules/
dist/

# Artefact généré par Style Dictionary (reconstruit par `npm run tokens`).
# On ne versionne pas une sortie dérivée : la source de vérité est tokens/tokens.json.
src/generated/

# Divers
.DS_Store
*.local
.vite/
