// Une implémentation qui existe, dans un langage que l'adaptateur TypeScript ne
// lit pas. Elle n'est jamais compilée ni exécutée : son seul rôle est d'EXISTER,
// pour que le test de T2.3 pose la question au disque plutôt qu'à un simulacre.
struct CibleNonReact {}
